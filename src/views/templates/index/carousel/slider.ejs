<!-- 轮播图组件 -->
<div class="slider-wrapper clearfix">
  <ul class="slider-list">
    <% for (var i = 0; i < sliderData.length; i++) { 
      var item = sliderData[i];  
    %>
      <%- include('item.ejs', { i, item }) %>
    <% } %>
  </ul>
</div>